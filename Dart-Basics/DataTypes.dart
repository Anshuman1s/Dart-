late String hello; // this is late variable if we decleared the late variable before accessing it need to initailize the value otherwise while accessing will get runtime error
void main(){
    int age=20;
    String name = "Anshuman";
    bool isLogin = false;
    double height = 5.10;


    int? val=13; // This int? mark means the val can store int value or null value 
    
}
