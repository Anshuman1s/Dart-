void main(){
    var x="Bob";
    x="Hey";
print(x);
}

// In dart Variables are -> var,dynamic,final,const 
// var-> var means Dart automatically detects the data type.
//dynamic-> dynamic means the variable can change type anytime.
// final->final means the variable value can be set only once.
// const ->before running the dart file const must be initialized(Must be known immediately).then after it will run otherwise at completime will give error
